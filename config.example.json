{
    "GithubToken": "",
    "RepoOwner": "Discord-Datamining",
    "RepoName": "Discord-Datamining",
    "RepoBranch": "heads/master",
    "ErrorMsg": "ono error",
    "Webhooks": [
        {
            "url": "",
            "send": {
                "plain": true, // plain is just sending new endpoints and nothing else
                "ping": true, // ping is sending a ping message when there is a new endpoint
                "errors": false, // If we want to log errors, rate limits etc
                "enabled": true // If we want to send anything at all
            },
            "pingMsg": "New Endpoint <@&1103522959022366832>" // Message to send when there is a new endpoint (linked to send.ping)
        }
    ],
    "Interval": 12000,
    "commitNewEndpoints": true, // if we should push the new json to the repo
    "commitNewEndpointsMsg": "[BOT] Updated endpoints", // commit message
    "Misc": {
        "id": "1096190356233670716"
    }
}
